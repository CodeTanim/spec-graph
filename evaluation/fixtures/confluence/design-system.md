# Product Interface Style

The interface uses a sky-blue palette, bold typography, direct labels, and clearly outlined interactive controls. Strong blue is reserved for selection and primary actions. Whitespace and quiet surfaces provide most of the visual grouping.

Dialogs can be closed with an explicit control or the Escape key. Interactive rows expose their expanded state and keyboard focus remains visible.
