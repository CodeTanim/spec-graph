# Data Retention

Disconnecting a source stops future refreshes and removes its current index and relationships. Historical review records remain available only when needed to explain prior findings and must not expose provider credentials.

Repeated versions are bounded so unchanged or obsolete content does not grow storage without limit. Deletion behavior should be deliberate, workspace-scoped, and free of orphaned evidence.
